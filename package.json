{
  "name": "tradesea-one-trade",
  "version": "2.3.0",
  "private": true,
  "description": "Local Chrome protection for one completed trade followed by an eight-hour account lockout",
  "scripts": {"test": "node --test tests/*.test.mjs"},
  "engines": {"node": ">=22"}
}
